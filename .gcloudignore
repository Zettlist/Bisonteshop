.gcloudignore
.git
.gitignore
node_modules/
.next/
# Essential build files - DO NOT IGNORE
#!Dockerfile
#!next.config.js
#!package.json
#!package-lock.json
# Wait, the ! syntax is for including, but I'll just remove them from the ignore list.
# Actually, I'll just list what to ignore.
.gcloudignore
.git
.gitignore
node_modules/
.next/
README.md
.dockerignore
pencil-welcome.pen
# Env local: NO subir al build (su NEXT_PUBLIC_BASE_URL=localhost pisaria a .env.production)
.env.local
.env*.local

# Copia entera del repo dentro del repo (368 MB, 439 archivos). No se usa para
# nada; sin esta linea, cada `gcloud run deploy --source .` la sube al build.
Bisonteshop/

# Credenciales. Sin esto, cada "gcloud run deploy --source ." sube el archivo
# con TODAS las contrasenas al bucket del constructor, donde queda guardado.
db/.credenciales-*.txt
*.credenciales*

# Carpetas de trabajo de Claude Code: una copia entera del repo en
# worktrees/ (208 MB). No se usa en el build y duplicaba lo que sube cada
# `gcloud run deploy --source .`.
.claude/
