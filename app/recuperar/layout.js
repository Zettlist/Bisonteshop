// Fuera de Google: es un paso de la compra, no una pagina que alguien busque.
export const metadata = {
    title: 'Recuperar contraseña',
    robots: { index: false, follow: false },
};

export default function Layout({ children }) {
    return children;
}
